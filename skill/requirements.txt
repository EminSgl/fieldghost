onnxruntime-gpu>=1.17  # falls back fine if you only have CPU, but expect it to be slow; swap for onnxruntime if you have no CUDA GPU at all
opencv-python>=4.8
numpy>=1.24
scipy>=1.11
pyyaml>=6.0
openvino>=2024.0  # only needed for embed.py if your re-id model is an OpenVINO IR (.xml/.bin) rather than .onnx
